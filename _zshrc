# Path to your oh-my-zsh configuration.
ZSH=$HOME/.oh-my-zsh

# Set name of the theme to load.
# Look in ~/.oh-my-zsh/themes/
# Optionally, if you set this to "random", it'll load a random theme each
# time that oh-my-zsh is loaded.
ZSH_THEME="prose"

function review() {
  hg pull
  if [ "$1" ]
  then
    hg up -C
    echo "hg merge $1"
    hg merge $1
  fi
  vim $( hg status | grep -v "R " | grep -v "?" |awk '{print $2}')
}

# Example aliases
# alias zshconfig="mate ~/.zshrc"
# alias ohmyzsh="mate ~/.oh-my-zsh"
alias tmuxn='tmux -2 new -s'
alias vi='vim'
alias clisp="rlwrap -f $HOME/.rlwrap/keywords clisp"

export PLATFORM=`python -c 'import os; print os.uname()[1],'`

export EDITOR=vim
# alias hop='ssh -L 9090:127.0.0.1:80 -l arjen.dijkstra 192.168.0.31'

# Set to this to use case-sensitive completion
# CASE_SENSITIVE="true"

# Uncomment this to disable bi-weekly auto-update checks
# DISABLE_AUTO_UPDATE="true"

# Uncomment to change how often before auto-updates occur? (in days)
# export UPDATE_ZSH_DAYS=13

# Uncomment following line if you want to disable colors in ls
# DISABLE_LS_COLORS="true"

# Uncomment following line if you want to disable autosetting terminal title.
# DISABLE_AUTO_TITLE="true"

# Uncomment following line if you want to disable command autocorrection
DISABLE_CORRECTION="true"

# Uncomment following line if you want red dots to be displayed while waiting for completion
# COMPLETION_WAITING_DOTS="true"

# Uncomment following line if you want to disable marking untracked files under
# VCS as dirty. This makes repository status check for large repositories much,
# much faster.
# DISABLE_UNTRACKED_FILES_DIRTY="true"

# Which plugins would you like to load? (plugins can be found in ~/.oh-my-zsh/plugins/*)
# Custom plugins may be added to ~/.oh-my-zsh/custom/plugins/
# Example format: plugins=(rails git textmate ruby lighthouse)
plugins=(autojump cabal colorize compleat django docker docker-compose git github jump lol mercurial pip python tmux vagrant virtualenv)

source $ZSH/oh-my-zsh.sh

# source /etc/profile.d/autojump.zsh
# source /usr/share/autojump/autojump.zsh

# autoload -U compinit && compinit -u


# if [[ -r /usr/local/lib/python2.7/site-packages/powerline/bindings/zsh/powerline.zsh ]]; then
#     source /usr/local/lib/python2.7/site-packages/powerline/bindings/zsh/powerline.zsh
# fi

# Customize to your needs...
# export PATH=$PATH:/home/arjen/.cabal/bin:/home/arjen/local/bin:/home/arjen/.local/bin:/home/arjen/local/bin:/home/arjen/.local/bin:/home/arjen/.cabal/bin:/home/arjen/bin:/home/arjen/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin:/usr/games:/usr/local/games:/usr/lib64:/home/arjend/Downloads/bin:/home/arjend/Downloads/bin:/usr/lib64
export PATH=/home/arjen/.cabal/bin:/home/arjen/bin:/home/arjen/local/bin:/home/arjen/.local/bin:$HOME/bin/neovim/bin:$HOME/bin/python3.5/:$PATH

# OPAM configuration
. /home/arjen/.opam/opam-init/init.zsh > /dev/null 2> /dev/null || true

if [ -f ENV/bin/activate ];
then
  source ENV/bin/activate
fi

alias clisp="rlwrap -H $HOME/.rlwrap/clisp_history -f $HOME/.rlwrap/keywords clisp"
alias hgg='hg glog'

# source /home/arjen/Programms/tmuxinator/completion/tmuxinator.zsh
# source /home/arjen/Programms/tmuxinator/completion/tmuxinator.bash
#

export JUMPHOSTUSER=adijkstra
export DTAP=dev
export VAGRANT_DEFAULT_PROVIDER=virtualbox

# vim: ft=zsh sw=2 ts=2 et
