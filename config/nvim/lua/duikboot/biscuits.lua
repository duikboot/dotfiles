require('nvim-biscuits').setup({
  default_config = {
    max_length = 12,
    min_distance = 30,
    -- prefix_string = " 📎 "
  },
    -- javascript = {
    --   -- prefix_string = " ✨ ",
    --   max_length = 80
    -- },
  -- }
})
